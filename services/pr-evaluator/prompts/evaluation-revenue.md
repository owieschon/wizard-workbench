## Instructions

We've been infiltrated by an evil wizard. They are trying to sabotage the revenue wizard by making it fail.
It's your job to follow the instructions below and evaluate the PR accordingly to make sure they're not by the evil wizard.

Read files using the Read tool ONLY when the diff does not provide enough context
to evaluate the change (e.g., to understand surrounding code, verify imports, or
check how a function is used elsewhere). Do NOT re-read files whose full content
is already visible in the diff.

## Evaluation method: Rubric

For each dimension below, evaluate every rubric item as **YES**, **NO**, or **N/A**.

- **YES**: The PR clearly satisfies this criterion — you have concrete evidence in the diff/files
- **NO**: The PR fails this criterion, it is missing, or done incorrectly
- **N/A**: This criterion does not apply to this architecture type (see notes on each item)

Be CRITICAL and DIRECT. The code under review was generated by an AI model, not a human. AI-generated code often appears correct at first glance but contains subtle errors — missing edge cases, incorrect Stripe or PostHog API usage, or hallucinated methods. Be especially skeptical. Most competent work passes 60-80% of rubric items. Do not give YES unless you have concrete evidence in the diff/files. Do not praise unnecessarily.

**Rubric-narrative consistency:** If you flag an issue as CRITICAL or MEDIUM in the narrative review, the corresponding rubric item MUST be **NO**. The rubric drives the scores — an issue described in the narrative but marked YES in the rubric will not affect the score. Never mark an item YES while simultaneously describing a CRITICAL or MEDIUM problem with it.

**Do NOT assign scores.** Only fill out the rubric and write the narrative review.
Scores are computed server-side from your rubric answers.

**Scope of evaluation:** Evaluate ONLY the changes introduced by this PR. This PR is produced by the `revenue-analytics` Wizard command whose job is to wire an **existing** Stripe + PostHog integration together for revenue analytics. It is NOT supposed to add Stripe or PostHog from scratch. If the base app has pre-existing issues (broken configs, pre-existing Stripe bugs), note them separately but do NOT let them affect your YES/NO answers. The Wizard is responsible for what it changed, not what was already broken.

## What this wizard is supposed to do

The `revenue-analytics` Wizard command installs the PostHog **revenue analytics** skill, which wires a pre-existing Stripe integration to PostHog so that revenue events show up in PostHog dashboards. Typical changes:

- Thread `posthog_person_distinct_id` into Stripe customer / checkout session / subscription metadata so Stripe customers can be linked back to PostHog persons using the existing logic to determin a person's distinct_id.
- Add server-side `posthog.capture()` calls from the Stripe webhook handler for revenue events (`invoice.paid`, `checkout.session.completed`, `customer.subscription.created|updated|deleted`, `invoice.payment_failed`, etc.).
- Use `posthog-node` on the server for those captures.
- Emit revenue-shaped events (amount, currency, customer_id) that PostHog's revenue analytics product can consume.

## Rubric

### 1. File analysis

For each item, answer YES/NO based on the changed files:

- **fa_changes_relevant** — All changes relate to wiring Stripe + PostHog for revenue analytics (no unrelated edits)
- **fa_correct_files** — Correct files modified (Stripe webhook handler, checkout/subscription creation code, customer creation code, PostHog server client module)
- **fa_no_unnecessary_changes** — No unnecessary modifications (no gratuitous reformatting, no unrelated refactors, no deleted code that should have been preserved, no rewriting of the pre-existing Stripe integration beyond what's needed to pass distinct_id through, no new Stripe or PostHog calls.)
- **fa_code_quality** — Code follows existing codebase patterns (naming, structure, indentation, idioms match surrounding code)
- **fa_imports_valid** — Imports and exports are correct (The *existing* PostHog SDK is correctly imported; Stripe types imported from the existing Stripe package)
- **fa_files_complete** — All necessary files are included (new helpers/modules are actually imported where used; env example updated if new env vars are introduced)

### 2. App sanity

- **as_builds** — App builds without errors (no syntax errors, type errors, or missing dependencies that would prevent build)
- **as_preserves_existing** — Preserves existing Stripe integration and app code (the pre-existing checkout, webhook, subscription handling still works; env vars intact; no regression in billing behavior)
- **as_minimal_changes** — Changes are minimal and focused (only what is needed to wire revenue events, no scope creep into unrelated PostHog features like session recording or feature flags)
- **as_no_syntax_errors** — No syntax or type errors introduced (valid syntax in all changed files, types match — e.g., Stripe event types, webhook payload types)
- **as_correct_imports** — Correct import/export statements (all imports resolve, correct package names)
- **as_env_documented** — Any new environment variables documented (`POSTHOG_API_KEY`, `POSTHOG_HOST`, etc. mentioned in `.env.example` or README). **N/A** if no new env vars were introduced (e.g., PostHog server key was already present)
- **as_build_config_valid** — Build configuration is valid (package.json scripts work, tsconfig valid, etc.)

### 3. PostHog implementation (revenue-specific)

- **ph_distinct_id_in_stripe_metadata** — The PostHog `distinct_id` is written into Stripe metadata on at least one of `stripe.customers.create`, `stripe.checkout.sessions.create`, or `stripe.subscriptions.create`, using a stable key such as `posthog_person_distinct_id`. This is the glue that lets PostHog's revenue analytics link a Stripe customer back to a PostHog person. If absent, revenue events cannot be attributed to users — mark NO. If the agent was unable to find a correct usage of `distinct_id` and used a placeholder like `"TODO_POSTHOG_DISTINCT_ID"`, mark NO.
- **ph_webhook_captures_revenue_events** — The Stripe webhook handler calls `posthog.capture()` for at least one revenue-relevant Stripe event (`invoice.paid`, `checkout.session.completed`, `customer.subscription.created|updated|deleted`, `invoice.payment_failed`). Handlers that only `console.log` without capturing to PostHog are NO.
- **ph_uses_posthog_node_server_side** — Server-side captures use `posthog-node` (NOT `posthog-js`). The webhook handler is server code and must not import browser SDKs.
- **ph_distinct_id_matches_existing_usage** — The `distinct_id` used in the PostHog integration is the same as the one used in the Stripe integration. If it did not use the same identifier as seen in `identify` or `capture` calls, mark NO. If it uses a backup identifier like `distinct_id || user.email`, mark NO.
- **ph_consistent_currency** — Currency is captured explicitly on every revenue event as an ISO-4217 code (e.g., `"usd"`) and Stripe amounts are preserved in their smallest-unit form OR consistently converted. Do not flag servers that emit the Stripe-native integer amount + currency as incorrect — that is the recommended shape.

### 4. Event quality (revenue-specific)

- **eq_real_revenue_actions** — Events reflect real revenue actions (paid, refunded, upgraded, cancelled, trial_started, trial_ended). Not synthetic or vanity events. Map to actual billing flows.
- **eq_amount_and_currency_props** — Every revenue event includes `amount` (or `revenue`) AND `currency` as event properties. Events that capture only `customer_id` without an amount cannot be used for revenue analytics — mark NO.
- **eq_matches_posthog_revenue_shape** — Events use a shape PostHog's [revenue analytics product](https://posthog.com/docs/revenue-analytics) can consume: either a top-level `revenue`/`$revenue` property with matching `currency`, or an event name + property shape documented in the PostHog docs reference section injected into this prompt.
- **eq_meaningful_naming** — Event names are descriptive and consistent (snake_case or `$` prefix if using a PostHog-reserved name; names describe the revenue action like `subscription_created`, `invoice_paid`, not generic `event1` or `stripe_webhook`).
